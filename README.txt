REALMFALL GitHub Pages FIXED

For GitHub Pages, use index.html in the ROOT of your repository.

This version fixes the lobby/START ASCENSION bug:
- Host state is created when the lobby opens.
- START ASCENSION now switches into the game correctly.
- Keyboard/mouse input starts correctly after entering the game.
- PeerJS is used for browser-to-browser lobby connections.

GitHub Pages is static, so there is no Node server running on GitHub Pages.
